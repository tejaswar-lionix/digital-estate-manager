import React, {useState} from 'react';
export const ExecutorView: React.FC = () => {
  const [filter,setFilter]=useState('high');
  return <div><h2>EXECUTOR - Executor - verify death, legal hold, tra</h2><p>verify</p></div>
};
export default ExecutorView;
